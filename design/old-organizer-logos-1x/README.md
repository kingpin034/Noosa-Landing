# Superseded 1x organizer logos

The original Figma export of the logo strip, at 1x. Replaced on 2026-09-06 by a
2x set (every file is exactly double the design box, e.g. `94x48` became
`188x96`), which renders sharply on retina displays at the same layout size.
Same fifteen logos in the same order.
